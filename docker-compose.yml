version: "3"

services:
    nginx:
        restart: unless-stopped
        build:
            context: .
            dockerfile: ./docker/nginx/Dockerfile
            args:
                - REACT_APP_GTAG=$GOOGLE_ANALYTICS_GTAG
                - REACT_APP_CURRENT_COMMIT_SHA=$CURRENT_COMMIT_SHA
        image: swotnginx
        ports:
            - 80:80
        depends_on:
            - backend
    backend:
        restart: unless-stopped
        build:
            context: .
            dockerfile: ./docker/backend/Dockerfile
        image: swotbackend
        expose:
            - 3000
        command: "npm start"
